name: test_timer_countup
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:

  cupertino_icons:

  flutter:
    sdk: flutter

  freezed_annotation:
  hooks_riverpod:
  json_annotation:

dev_dependencies:

  build_runner:
  flutter_lints:

  flutter_test:
    sdk: flutter

  freezed:
  json_serializable:

flutter:

  uses-material-design: true
